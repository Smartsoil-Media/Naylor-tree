# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Original HEIC source photos & local scratch — these are large (~230MB total)
# and the web-ready JPGs in public/images/sources/ are what the deployed site
# actually uses. Keep context.md tracked though — it's the digest of these.
source documents/*
!source documents/context.md

# Original family media (HEIC/MOV) — optimised copies live in public/images/people/
family_images/

# One-off screenshot dropped at the repo root
tree.png
